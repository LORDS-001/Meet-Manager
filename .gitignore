.venv/
__pycache__/
*.pyc

# Secrets and local state - never commit these
.env
credentials.json
token.json
data/

# Test tooling and output (the lockfile IS committed, so CI can use `npm ci`)
node_modules/
shots/
